name: Auto Reviewers
on:
  pull_request:
    types: [opened, ready_for_review]

jobs:
  add-reviews:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@v2.0.0
        with:
          reviewers: 'devjaesung,chen4023' # 리뷰어 깃헙 아이디
